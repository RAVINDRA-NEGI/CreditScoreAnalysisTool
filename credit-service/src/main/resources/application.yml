server:
  port: 8090
spring:
  application:
    name: "credit-service"

  datasource:
    url: jdbc:mysql://localhost:3306/creditservice
    username: root 
    password: chutiya22
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8070/eureka